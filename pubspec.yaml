name: clima
description: A Flutter project to display weather information.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.17.6 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.3  # Dependencia para hacer peticiones HTTP

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
